{
  "name": "worstbillionaires-e2e",
  "version": "1.0.0",
  "description": "End-to-end tests for Worst Billionaires 2025",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:smoke": "playwright test smoke.spec.ts",
    "test:local": "BASE_URL=http://localhost:5173 playwright test",
    "test:prod": "BASE_URL=https://worstbillionaires.com playwright test smoke.spec.ts"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "typescript": "^5.9.3"
  }
}
